version: '3.8'

services:
  abc-tester:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: abc-tester
    ports:
      - '3000:3000'
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/app/data/abc-tester.db
      - NEXT_PUBLIC_APP_NAME=ABC Tester
    volumes:
      - abc-tester-data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'wget', '--no-verbose', '--tries=1', '--spider', 'http://localhost:3000']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  abc-tester-data:
    driver: local
